apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-duckduckgo
  namespace: mcp-servers
  labels:
    app: mcp-duckduckgo
    app.kubernetes.io/part-of: code-assistant-lab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-duckduckgo
  template:
    metadata:
      labels:
        app: mcp-duckduckgo
    spec:
      containers:
        - name: duckduckgo
          image: registry.access.redhat.com/ubi9/python-311:latest
          command: ["sh", "-c"]
          args:
            - |
              pip install -q duckduckgo-mcp-server &&
              npx -y supergateway \
                --stdio 'duckduckgo-mcp-server' \
                --outputTransport streamableHttp \
                --port 3006 \
                --healthEndpoint /health \
                --name duckduckgo
          env:
            - name: HOME
              value: /tmp
            - name: NPM_CONFIG_CACHE
              value: /tmp/.npm
          ports:
            - containerPort: 3006
              protocol: TCP
          startupProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 20
            periodSeconds: 5
            failureThreshold: 36
          livenessProbe:
            httpGet:
              path: /health
              port: 3006
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 3006
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 768Mi
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-duckduckgo
  namespace: mcp-servers
  labels:
    app: mcp-duckduckgo
spec:
  selector:
    app: mcp-duckduckgo
  ports:
    - port: 3006
      targetPort: 3006
      protocol: TCP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: mcp-duckduckgo
  namespace: mcp-servers
spec:
  to:
    kind: Service
    name: mcp-duckduckgo
  port:
    targetPort: 3006
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
