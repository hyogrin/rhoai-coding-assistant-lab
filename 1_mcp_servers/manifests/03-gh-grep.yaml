apiVersion: v1
kind: ConfigMap
metadata:
  name: gh-grep-server-script
  namespace: mcp-servers
data:
  server.js: |
    const { McpServer } = require("@modelcontextprotocol/sdk/server/mcp.js");
    const { SSEServerTransport } = require("@modelcontextprotocol/sdk/server/sse.js");
    const { z } = require("zod");
    const http = require("http");

    const server = new McpServer({ name: "gh-grep", version: "1.0.0" });

    server.tool(
      "search_code",
      "Search code across GitHub repositories",
      {
        query: z.string().describe("Search query (GitHub code search syntax)"),
        language: z.string().optional().describe("Filter by programming language"),
        org: z.string().optional().describe("Filter by organization"),
        maxResults: z.number().optional().default(10).describe("Max results (default: 10)"),
      },
      async ({ query, language, org, maxResults }) => {
        let q = query;
        if (language) q += ` language:${language}`;
        if (org) q += ` org:${org}`;

        const url = `https://api.github.com/search/code?q=${encodeURIComponent(q)}&per_page=${maxResults || 10}`;
        const res = await fetch(url, {
          headers: {
            Authorization: `token ${process.env.GITHUB_TOKEN}`,
            Accept: "application/vnd.github.v3+json",
          },
        });

        if (!res.ok) {
          return { content: [{ type: "text", text: `GitHub API error: ${res.status}` }] };
        }

        const data = await res.json();
        const results = data.items.map((item) => ({
          repo: item.repository.full_name,
          path: item.path,
          url: item.html_url,
        }));

        return {
          content: [{ type: "text", text: JSON.stringify(results, null, 2) }],
        };
      }
    );

    const PORT = process.env.GH_GREP_PORT || 3003;
    const httpServer = http.createServer();
    const transport = new SSEServerTransport("/sse", httpServer);

    httpServer.listen(PORT, () => {
      console.log(`gh-grep MCP server running on http://localhost:${PORT}/sse`);
      server.connect(transport);
    });
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-gh-grep
  namespace: mcp-servers
  labels:
    app: mcp-gh-grep
    app.kubernetes.io/part-of: code-assistant-lab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-gh-grep
  template:
    metadata:
      labels:
        app: mcp-gh-grep
    spec:
      containers:
        - name: gh-grep
          image: node:20-slim
          command: ["sh", "-c"]
          args:
            - |
              cd /tmp && npm install @modelcontextprotocol/sdk zod && NODE_PATH=/tmp/node_modules node /app/server.js
          workingDir: /tmp
          ports:
            - containerPort: 3003
              protocol: TCP
          env:
            - name: HOME
              value: /tmp
            - name: NPM_CONFIG_CACHE
              value: /tmp/.npm
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: mcp-api-keys
                  key: GITHUB_TOKEN
            - name: GH_GREP_PORT
              value: "3003"
          volumeMounts:
            - name: server-script
              mountPath: /app/server.js
              subPath: server.js
          livenessProbe:
            tcpSocket:
              port: 3003
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: server-script
          configMap:
            name: gh-grep-server-script
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-gh-grep
  namespace: mcp-servers
  labels:
    app: mcp-gh-grep
spec:
  selector:
    app: mcp-gh-grep
  ports:
    - port: 3003
      targetPort: 3003
      protocol: TCP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: mcp-gh-grep
  namespace: mcp-servers
spec:
  to:
    kind: Service
    name: mcp-gh-grep
  port:
    targetPort: 3003
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
